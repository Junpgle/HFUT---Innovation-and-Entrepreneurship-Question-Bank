# 数据报表功能测试指南

## 新增功能概览

Report.jsx（数据报表页面）现已新增两个功能板块：
1. **我的评论** - 展示并管理用户发表的评论
2. **我的解析** - 展示并管理用户贡献的解析

## 功能使用说明

### 前提条件
- 已登录的用户账号
- 邮箱已绑定且已验证
- 至少发表过一条评论或解析

### 访问报表

1. 在刷题页面点击右上角的"报表"按钮
2. 或直接访问 `/report.jsx`
3. 如未登录或邮箱未验证，会显示访问限制提示

### 我的评论板块功能

**位置**：页面顶部，日期选择器下方

**基本操作**：
```
┌─────────────────────────────────────┐
│ 💬 我的评论                          │
├─────────────────────────────────────┤
│ [题目 L1-1] · 👍 5                   │
│ 这道题的关键是理解XXX的含义...      │
│ 2026-01-15  编辑  删除              │
│                                     │
│ [题目 L2-5] · 👍 12                  │
│ 容易与第一讲的内容混淆，需要重点...  │
│ 2026-01-14  编辑  删除              │
└─────────────────────────────────────┘
```

**具体操作**：

1. **查看点赞者**
   - 点击点赞数 (👍 5)
   - 显示点赞者列表
   - 再次点击折叠

2. **编辑评论**
   ```
   [编辑] → 弹出编辑框 → 修改内容 → [保存] 或 [取消]
   ```

3. **删除评论**
   ```
   [删除] → 确认对话框 → [确定删除] → 列表自动更新
   ```

4. **跳转到题目**
   - 点击题目ID (L1-1)
   - 打开题目详情弹窗
   - 查看该题的所有评论和解析

### 我的解析板块功能

**位置**：紧跟在"我的评论"下方

**基本操作**：
```
┌─────────────────────────────────────┐
│ ⚡ 我的解析                          │
├─────────────────────────────────────┤
│ 题目 L1-1 · 👍 8                     │
│ ### 解题思路                        │
│ 这道题考查的是...                   │
│ > 第一步：理解概念                   │
│                                     │
│ 2026-01-15  编辑  删除              │
├─────────────────────────────────────┤
│ 题目 L3-12 · 👍 3                    │
│ ## 关键点                           │
│ - 容易混淆的地方                    │
│ - 与前面知识的联系                   │
│                                     │
│ 2026-01-12  编辑  删除              │
└─────────────────────────────────────┘
```

**具体操作**：

1. **查看点赞者**
   ```
   点击点赞数 (👍 8) → 显示点赞者名单 → 再次点击折叠
   ```

2. **编辑解析**
   ```
   [编辑] → 编辑框（支持Markdown）→ 修改内容 → [保存]
   ```
   支持的Markdown格式：
   - `# 标题`、`## 二级标题`等
   - `- 列表项`、`* 列表项`
   - `> 引用`
   - `**加粗**`、`*斜体*`
   - 代码块等

3. **删除解析**
   ```
   [删除] → 确认 → 自动从列表删除
   ```

4. **跳转查看题目**
   - 点击题目ID (L1-1)
   - 在弹窗中查看完整题目
   - 查看其他用户的解析

## 数据流向说明

### 数据保存流程
```
用户操作 
  ↓
前端验证 
  ↓
LeanCloud数据库 
  ↓
云函数处理（权限验证）
  ↓
数据持久化
```

### 点赞者列表
- 存储在评论/解析对象的 `likedBy` 数组中
- 显示用户名列表
- 由云函数 `likeComment` 和 `likeExplanation` 管理

## 常见问题

### Q1：为什么看不到"我的评论"和"我的解析"？
**A**：可能原因：
- 还未发表任何评论或解析
- 页面还在加载中（查看"正在同步云端数据..."提示）
- 登录状态已过期，需要重新登录

### Q2：编辑后多久才能生效？
**A**：编辑内容会立即保存到数据库，页面同时更新，无需刷新。

### Q3：删除的评论/解析能恢复吗？
**A**：一旦确认删除，数据将永久删除，无法恢复。请谨慎操作。

### Q4：点赞数是否包括自己的点赞？
**A**：是的，自己对自己的内容点赞也会计入。

### Q5：能看到别人的评论和解析吗？
**A**：
- 在题目详情弹窗中可以看到该题的所有评论和解析
- 在错题排行榜中可以看到其他用户贡献的解析
- 不能在报表页面编辑他人的内容

## UI布局说明

### 响应式设计
- **桌面端**：两列布局，容器较宽
- **平板/手机**：单列堆叠，自动调整内边距

### 卡片样式
| 元素 | 样式 |
|------|------|
| 我的评论 | 白色背景，slate-200边框 |
| 我的解析 | 紫蓝背景（indigo-50），indigo-200边框 |
| 编辑框 | 灰色边框，focus时蓝色高亮 |
| 按钮 | 蓝色(操作)/灰色(取消)/红色(删除) |

## 技术细节

### 关键函数

1. **handleUpdateComment(commentId)**
   - 保存评论修改
   - 自动刷新列表
   - 处理错误提示

2. **handleDeleteComment(commentId)**
   - 需要用户确认
   - 从LeanCloud删除
   - 同时更新本地状态

3. **handleUpdateExp(questionId)**
   - 保存解析修改
   - 重新加载该题的所有解析
   - 清空编辑状态

4. **handleDeleteExplanation(explanationId)**
   - 需要用户确认
   - 从LeanCloud删除
   - 更新本地列表

5. **renderMyCommentsSection()**
   - 条件渲染评论列表
   - 管理编辑和点赞者展示状态
   - 提供操作按钮

6. **renderMyExplanationsSection()**
   - 条件渲染解析列表
   - 使用Markdown渲染内容
   - 管理编辑和点赞者展示状态

### 状态管理
```javascript
// 用户评论和解析列表
const [userComments, setUserComments] = useState([]);
const [userExplList, setUserExplList] = useState([]);

// 编辑状态
const [editingCommentId, setEditingCommentId] = useState(null);
const [editingCommentContent, setEditingCommentContent] = useState('');
const [editingExpId, setEditingExpId] = useState(null);
const [editingExpContent, setEditingExpContent] = useState('');

// 展开状态
const [showCommentLikes, setShowCommentLikes] = useState(null);
const [showExplanationVotes, setShowExplanationVotes] = useState(null);
```

## 与其他功能的集成

### 与刷题页面的联系
- 用户在刷题时发表的评论和解析
- 会自动出现在数据报表的相应板块

### 与题目详情弹窗的联系
- 点击题目ID可跳转到该题
- 在弹窗中可查看完整题目、所有评论和解析

### 与点赞功能的联系
- 点赞由云函数`likeComment`和`likeExplanation`管理
- 一人只能最多点一次赞
- 再次点击自动取消点赞

## 性能优化建议

1. **分页加载**
   - 当评论/解析数量过多时，建议实现分页
   - 避免一次加载过多数据

2. **虚拟列表**
   - 对于超过100条的列表，考虑使用虚拟列表
   - 提升滚动性能

3. **缓存策略**
   - 首次加载后可缓存数据
   - 仅在新增/修改/删除时更新

## 已知限制与改进方向

1. **当前版本**
   - 不支持排序（按日期/点赞数）
   - 不支持搜索和筛选
   - 点赞者列表无跳转功能

2. **建议改进**
   - 添加排序功能（最新/最热）
   - 按题目或日期范围筛选
   - 点赞者可点击查看用户信息
   - 支持批量删除
   - 评论/解析导出功能

